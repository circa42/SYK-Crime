{% extends "_layouts/base" %}

{% block content %}
<div class="container mx-auto my-12">
    <h1 class="text-4xl text-bold font-black text-center uppercase bg-yellow-300 px-4 py-2 inline-block mb-12">{{ entry.title }}</h1>
    <article class="ck-content">
        {{ entry.body | raw }}
    </article>
</div>
{% endblock %}