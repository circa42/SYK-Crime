{% extends '_layouts/base' %}

{% block content %}



<div class="h-dvh xs:mx-12 px-10 md:mx-auto sm:container my-12">
  {# Query the homepage entry, assuming it's a Single entry type #}
    {% set entry = craft.entries.section('home').one() %}

    {# Check if the entry exists #}
    {% if entry %}
    <!--
          <h1 class="xs:text-xl md:text-2xl lg:text-4xl text-bold font-black text-center uppercase bg-yellow-300 px-4 py-2 inline-block mb-12">{{ siteName}}</h1>
    -->
          <article class="ck-content mb-12">
              {{ entry.body | raw }}
          </article>

    {% else %}
        <p>No content found for the homepage.</p>
    {% endif %}


{#

  <!-- Fetch first 50 entries from the books section -->
    {% set entries = craft.entries()
      .section('books')
      .limit(10)
      .all() 
    %}

    <!-- Loop through entries -->
    {% for entry in entries %}
        {% include "_includes/book" %}
    {% else %}
      <p>No books found.</p>
    {% endfor %}
     #}
    
     {#
      {% include "_includes/form" %}
      #}
    </div>
{% endblock %}


{#

{% extends '_layouts/base' %}

{% block content %}
<div class="container mx-auto border-blue-500 border-2 my-12">
  <!-- Fetch entries with pagination support -->
  {% set page = craft.app.request.getQueryParam('page', 1) %}
  {% set entries = craft.entries()
    .section('books')
    .limit(10)
    .offset((page - 1) * 10)
    .all() 
  %}

  <!-- Loop through entries -->
  <div id="entries">
    {% for entry in entries %}
      {% include "_includes/book" %}
    {% else %}
      <p>No books found.</p>
    {% endfor %}
  </div>
  
  <!-- Placeholder for more entries -->
  <div id="loading" style="display: none;">Loading more entries...</div>
</div>
{% endblock %}
#}